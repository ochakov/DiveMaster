<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />
    <!-- Depth comes from the barometer; the app is meaningless without one. -->
    <uses-feature
        android:name="android.hardware.sensor.barometer"
        android:required="true" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Samsung private depth/water-temp sensors (Galaxy Watch Ultra). Signature|privileged:
         granted only to platform-signed/privileged installs; everywhere else the sensors read
         null and the app uses the barometer. Declaring it is harmless on other watches. -->
    <uses-permission
        android:name="com.samsung.permission.SSENSOR"
        tools:ignore="ProtectedPermissions" />
    <!-- Samsung Water Lock (Galaxy Watch): keeps the screen usable and disables the
         touchscreen underwater. Best-effort — likely privileged; the app works without it. -->
    <uses-permission
        android:name="com.samsung.android.clockwork.settings.permission.CHANGE_WATER_LOCK"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".DiveMasterApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.DiveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="dive-computer-tracking" />
        </service>
    </application>

</manifest>
